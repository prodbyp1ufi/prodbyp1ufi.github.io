export default class Board{
    constructor(id, name,sortIndex, workspaceId){
        this.id = id
        this.name = name
        this.sortIndex = sortIndex
        this.workspaceId = workspaceId
    }
}
