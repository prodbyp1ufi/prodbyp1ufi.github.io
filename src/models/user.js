export default class User{
    constructor(id, avatarURL, displayName, email){
        this.id = id
        this.avatarURL = avatarURL
        this.displayName = displayName
        this.email = email
    }
}